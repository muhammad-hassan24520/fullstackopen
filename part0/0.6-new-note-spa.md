sequenceDiagram
participant browser
participant server

Note right of browser: User types a new note

browser->>server: POST /exampleapp/new_note_spa (JSON payload)
activate server
server-->>browser: 201 Created (JSON response)
deactivate server

Note right of browser: Server does NOT redirect, page stays same

Note right of browser: JavaScript updates UI dynamically (DOM manipulation)

Note right of browser: Existing notes list updated in browser memory

browser->>server: (optional later) GET data.json
server-->>browser: JSON data (fresh sync if needed)
